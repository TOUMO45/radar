{
  "name": "@scenelock/gate-clearance",
  "version": "0.1.0",
  "description": "Clearance gate (spec E.5.2): trademark · lyrics · real_person · ai_disclosure. Deterministic cores + schema-validated explainer. Emits Finding v2 + gate_run.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@scenelock/ports": "workspace:*",
    "@scenelock/schema": "workspace:*"
  },
  "devDependencies": {
    "@scenelock/fixtures": "workspace:*",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
